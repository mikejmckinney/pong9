# Docker ignore file
# Prevents unnecessary files from being copied to Docker context

# Dependencies (will be installed in container)
node_modules/
**/node_modules/

# Build outputs (will be rebuilt in container)
dist/
**/dist/

# Compiled JS/TS outputs - be specific to avoid excluding config files
# Fix for: "*.js exclusion too broad" - @gemini-code-assist suggestion
# https://github.com/PR#comment
# Only exclude .js/.d.ts in source directories, not root config files
client/src/**/*.js
server/src/**/*.js
shared/*.js
*.js.map
client/src/**/*.d.ts
server/src/**/*.d.ts
shared/*.d.ts

# Git
.git/
.gitignore

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo

# Documentation (not needed in container)
.context/
.cursor/
.gemini/
.github/
*.md
!README.md

# Test files
test.sh
install.sh

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Local env files
*.local
.env.local
.env.*.local
